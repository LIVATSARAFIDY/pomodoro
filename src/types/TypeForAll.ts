import { EnumType } from "typescript"

export type TypeTask = {
    id:number,
    text:string,
    active:boolean,
    nbSessionPomo:number,
    nbPomoEffectif:number,
    note:string,
    status:boolean
}
